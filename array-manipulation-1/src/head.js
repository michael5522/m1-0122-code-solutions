/* exported head */
// I will create a for loop and then use the index then return the result

function head(array) {
  return array[0];
}

// define a function named head which has a single parameter `array`
// return array[0]
