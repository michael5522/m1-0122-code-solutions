/* exported setValue */
// I will use dot notation to add the key,value pair onto the object in the function.

function setValue(object, key, value) {
  object[key] = value;
}

// create a function named setValue with 3 parameters object, key, and value
// use the bracket notation to set the value and createa a new property
