/* exported getValue */
// I will create a function and then target the value of the object and extract it

function getValue(object, key) {

  return object[key];
}

// create a function named getValue with two parameeters object and key
// return using object key property
