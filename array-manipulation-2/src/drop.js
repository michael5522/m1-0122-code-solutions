/* exported drop */

// i will create an function that will leave out the "count" and return it
function drop(array, count) {
  var newArray = array.slice(count);
  return newArray;
}
// create a function named drop with 2 parameters
// create a new array named newArry that slices the "count" and removes it and saves it
// return the newArray;
