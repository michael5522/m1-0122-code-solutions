/* exported last */
// i will create a function that uses the array.length-1 to get the last element of array.

function last(array) {
  return array[(array.length - 1)];
}

// define a function named last with a single parameter `array`
// return the value of the index of array at (array.length-1)
